import { Container } from "react-bootstrap"

const Footer = (props) => (
  <div className="w-100 navbar-color">
    <Container className="mt-5">
      <p className="text-center mb-0 py-2">Why hello there</p>
    </Container>
  </div>
)

export default Footer